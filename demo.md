# demo.md —— 答辩演示动线（约 8 分钟）

> 前置：双击 `start.bat`（自动拉起后端 8000 + 前端 5173 并打开浏览器）；
> 如需把数据还原到种子状态，先双击 `reset-db.bat` 再演示。
> 测试账号：`admin` / `officer` / `user`，密码均为 `123456`。

## 演示路线总览

```
登录（验证码） → 配时计算（Webster 亮点） → 违章录入与审核（状态流转）
→ 车牌识别（AI 加分） → 出入场结算（计费引擎） → 数据可视化大屏（门面收尾）
```

---

## 1. 登录与权限体系（1 分钟）

1. 登录页输入 `admin / 123456`，**点击验证码图片看不清可刷新**，输入验证码登录；
2. 指出：登录走 JWT（2 小时过期）+ Redis 验证码（TTL 300s）+ 登录失败 5 次锁定 10 分钟（可现场输错密码演示锁定提示）；
3. 提到右侧角色标签与菜单：**不同角色登录后菜单不同**（后面第 5 步演示）。

## 2. Webster 配时计算（2 分钟，答辩算法亮点 ①）

1. 侧边栏「交通管理 → 信号配时」；
2. 左侧计算器输入四个相位流量（默认 1200/400/1000/350）与车道数，点击「**计算最优周期**」；
3. 讲解输出：`周期 C0=…s`、各相位绿灯按流量比分配、最短绿灯 15s 约束；把东西直行流量调大到 3000 演示**过饱和**（Y≥0.95 → 上限 180s）；
4. 点击「保存为配时方案」并启用，右侧列表展示定周期 / 感应自适应两种模式与绿信比；
5. 强调：算法为**纯函数**（不碰数据库），前后端同源实现 + 双侧单元测试；参数口径统一 s=1800、每相位损失 6s、周期钳制 [40,180]。

## 3. 违章录入与审核（1.5 分钟）

1. 「车辆违章 → 违章管理」→「录入违章」：选车牌（或手输 `京A12345`）、类型选「闯红灯」——罚款 200 / 记 6 自动带出，提交后状态「待审核」；
2. 列表上对该记录「通过」审核 → 状态「已确认」→ 再「标记已处理」→ 状态「已处理」；
3. 讲解状态流转：`pending → confirmed/rejected → processed`，审核人/时间/备注全留痕（可在「系统管理 → 操作日志」看到本次操作）。

## 4. 车牌识别 + 停车计费（1.5 分钟，算法亮点 ②③）

1. 「智能工具 → 车牌识别」：上传一张车辆图片（演示目录 `backend/uploads/lpr_test.png` 可用），点击「开始识别」→ 显示车牌号与置信度（HyperLPR3 / onnxruntime **CPU 推理**）；
2. 点击「复制车牌号」→「智慧停车 → 出入场记录」→「车辆入场」粘贴车牌（可附入场拍照）；
3. 再点「车辆出场」同车牌 → 现场结算费用；讲解计费规则：免费时长 → 首小时 → 每小时（向上取整）→ 单日封顶（每 24h 一段），核心函数纯函数 + 单测；
4. 若识别失败（无网络首次下载模型超时），演示降级路径：界面引导「手动录入车牌」——文档已注明。

## 5. 角色差异（30 秒）

1. 退出登录，改用 `officer / 123456` 登录 → 侧边菜单无「系统管理」，业务功能可操作；
2. 再用 `user / 123456` 登录 → 只剩大屏、路况、本人车辆违章、停车余位、公告反馈（后端接口 403 兜底，不只是隐藏按钮）。

## 6. 数据可视化大屏（1.5 分钟，门面收尾）

1. 任意角色点击「数据可视化大屏」（独立全屏页，1920×1080 **scale 等比缩放**，借鉴 go-view）；
2. 逐图讲解：
   - 全市车流量 24 小时趋势：**早晚双峰**（7:30–9:00 / 17:30–19:00）形态真实；
   - 各路口实时拥堵态势：8 个路口地图散点（模拟路网坐标），颜色对应四级拥堵；
   - 信号灯状态分布、违章类型 TOP5（近 7 天）、停车场占用率横向条形；
   - 顶部 4 个核心指标卡（今日车流量/平均车速/违章/待处理投诉）；
3. 强调机制：前端 **10 秒轮询** `/dashboard/realtime`，后端 **Redis 缓存 TTL 8 秒**；
4. **现场演示降级**（提前彩排）：关闭后端窗口 → 大屏 10 秒内出现「后端服务连接失败」全屏提示与「重新连接」按钮；重启后端 → 自动恢复数据；
5. 收尾：模拟任务由 APScheduler 驱动（每分钟流量上报、每 10 秒信号相位推进），`.env` 的 `MOCK_DATA_ENABLED=false` 可一键关闭。

---

## 备答要点（Q&A 弹药）

| 可能提问 | 回答要点 |
|---|---|
| Webster 公式参数依据？ | s=1800 pcu/h/车道（附录 D 统一），每相位损失=启动损失 3s+黄灯 3s（含全红 1s），C0=(1.5L+5)/(1−Y) |
| 为什么不用微服务？ | 课程单体够用且本机无容器环境；但严格三层分离（router→service→model），接口按 /api/v1 规划，未来可平滑拆分 |
| 密码/密钥安全？ | bcrypt 存储（钉死 4.0.1 规避 passlib 兼容）；密钥全走 .env，仓库只有 .env.example；JWT 登出进 Redis 黑名单 |
| 测试怎么保证？ | pytest 85 例（独立库 smart_traffic_test，不污染正式库）+ vitest 14 例 + ruff/eslint 零错误；详见 docs/04-测试报告 |
| 数据真实感？ | seed.sql 由 scripts/gen_seed.py 生成（固定随机种子）：30 天小时流量带早晚高峰/周末衰减/周五加成、520 违章、300 出入场；日期可重新生成刷新 |
| 时间时区？ | 全链路 Asia/Shanghai 本地时间，库内一律 DATETIME，避免 8 小时偏差 |
