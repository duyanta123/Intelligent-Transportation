# 智慧交通综合管理服务平台 —— 系统设计说明书

> 版本：1.0　|　日期：2026-09-20　|　配套：《01-需求规格说明书》《03-数据库设计说明书》

---

## 1. 总体架构

### 1.1 架构图（分层单体，三层分离）

```mermaid
graph TB
    subgraph 浏览器端
        BS[数据可视化大屏<br/>scale 等比缩放]
        MP[管理端页面<br/>侧边菜单 + 面包屑]
    end

    subgraph 前端 Vue3 + Vite + TS
        RT[Vue Router 路由守卫]
        ST[Pinia 状态管理]
        AX[Axios 拦截器<br/>JWT 注入/统一错误]
        EP[Element Plus 组件库]
        EC[ECharts 5 图表]
        RT --> ST --> AX
        MP --> EP
        BS --> EC
    end

    subgraph 后端 FastAPI（app/）
        R[routers 路由层<br/>auth/system/signal/traffic/<br/>violation/parking/lpr/service/dashboard/admin]
        S[services 服务层<br/>algorithms 纯函数/Webster·拥堵·计费<br/>auth_service/oplog/lpr_service]
        M[models 模型层<br/>SQLAlchemy 2.x · 18 张表]
        T[tasks 定时任务<br/>APScheduler 模拟数据]
        C[core 基础设施<br/>config/database/redis/security/deps]
        R --> S --> M
        R --> C
        T --> M
    end

    DB[(MySQL 8<br/>smart_traffic)]
    RD[(Redis 5<br/>验证码/黑名单/限流/大屏缓存)]
    FS[/backend/uploads<br/>入场拍照/识别图片/]

    MP -->|/api/v1 RESTful| R
    BS -->|10s 轮询| R
    M --> DB
    C --> RD
    FS -->|静态回显 /static/uploads| MP
```

### 1.2 技术选型理由

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | Vue 3（组合式 API）+ TypeScript | 渐进式、组合式逻辑复用好；TS 提供编译期类型保障 |
| 构建 | Vite 6 | 原生 ESM 秒级冷启动，Node 22 完全兼容 |
| UI 组件 | Element Plus | 企业级中后台组件齐全、全中文文档 |
| 图表 | ECharts 5 | 国产开源、geo 地图/大屏能力强，go-view 同款 |
| 状态 | Pinia | Vue3 官方推荐，TS 友好 |
| 后端框架 | FastAPI | 异步高性能、自动生成 /docs 接口文档、Pydantic 校验 |
| ORM | SQLAlchemy 2.x | Python 事实标准，2.0 风格 typed mapped_column |
| 数据库 | MySQL 8（utf8mb4_general_ci） | 课程要求与本地实测环境；关系型适合业务数据 |
| 缓存 | Redis 5.0.14 | 验证码/黑名单/限流/大屏缓存；仅使用 5.x 支持命令（RESP2） |
| 认证 | JWT（python-jose）+ bcrypt 4.0.1 | 无状态认证；bcrypt 钉死 <4.1 规避 passlib 兼容问题 |
| 定时任务 | APScheduler 3.10 | 进程内调度，无需外部中间件 |
| 车牌识别 | HyperLPR3（onnxruntime CPU） | 中文车牌识别 95%+，纯 CPU 可跑，免 GPU |

### 1.3 接口设计规范

- 统一前缀 `/api/v1`，统一响应结构 `{code: int, message: str, data: any}`；
- HTTP 状态码照常表达 401/403/404/422/500；业务 code 分段：`0` 成功、`1xxxx` 用户与权限、`2xxxx` 交通业务、`3xxxx` 停车与计费、`4xxxx` 公共服务、`9xxxx` 系统错误；
- 分页参数 `page/size`，返回 `{list, total, page, size}`；
- 完整接口清单见《智慧交通-项目开发Prompt.md》附录 C，实现一致，运行时可在 `http://127.0.0.1:8000/docs` 查看 Swagger。

---

## 2. 数据模型设计（E-R 图）

### 2.1 E-R 图

```mermaid
erDiagram
    USER ||--o{ USER_ROLE : 拥有
    ROLE ||--o{ USER_ROLE : 被授予
    ROLE ||--o{ ROLE_MENU : 授权
    MENU ||--o{ ROLE_MENU : 被授权
    MENU ||--o{ MENU : "父子(自关联)"

    USER ||--o{ VEHICLE : 登记
    USER ||--o{ FEEDBACK : 提交
    USER ||--o{ OP_LOG : 产生

    INTERSECTION ||--o{ SIGNAL_PLAN : 配置
    INTERSECTION ||--o{ SIGNAL_STATUS : "1:1 实时状态"
    INTERSECTION ||--o{ ROAD_SECTION : "起/止"
    ROAD_SECTION ||--o{ TRAFFIC_FLOW : 采集

    VEHICLE ||--o{ VIOLATION : 关联
    INTERSECTION ||--o{ VIOLATION : 发生地点

    PARKING_LOT }o--|| FEE_RULE : 套用
    PARKING_LOT ||--o{ PARKING_RECORD : 产生

    NOTICE }o--|| USER : "发布者"

    USER {
        int id PK
        string username UK
        string password_hash "bcrypt"
        int status
    }
    ROLE {
        int id PK
        string code UK "admin/officer/user"
    }
    MENU {
        int id PK
        int parent_id
        string path
    }
    INTERSECTION {
        int id PK
        string name
        decimal longitude
        decimal latitude
        int lane_count
        string district
    }
    SIGNAL_PLAN {
        int id PK
        int intersection_id FK
        string mode "fixed/adaptive"
        int cycle_seconds
        json phases
        decimal green_ratio
        int is_active
    }
    SIGNAL_STATUS {
        int id PK
        int intersection_id FK "UNIQUE"
        string current_phase
        int remaining_seconds
    }
    ROAD_SECTION {
        int id PK
        string name
        int lane_count
        int capacity "车道数×600"
    }
    TRAFFIC_FLOW {
        bigint id PK
        int road_section_id FK
        datetime recorded_at "索引"
        int flow "pcu/h"
        decimal saturation "v/c"
        int congestion_level "0-3"
    }
    VEHICLE {
        int id PK
        int user_id FK
        string plate_no UK
    }
    VIOLATION {
        int id PK
        string plate_no
        string violation_type
        string status "pending/confirmed/rejected/processed"
        int audit_by
    }
    FEE_RULE {
        int id PK
        int free_minutes
        decimal first_hour_fee
        decimal hourly_fee
        decimal daily_cap
    }
    PARKING_LOT {
        int id PK
        int total_slots
        int used_slots
        int fee_rule_id FK
    }
    PARKING_RECORD {
        int id PK
        int parking_lot_id FK
        string plate_no
        datetime enter_time
        datetime exit_time
        decimal fee
        string status "inside/finished"
    }
    NOTICE {
        int id PK
        string title
        int status
    }
    FEEDBACK {
        int id PK
        int user_id FK
        string status "pending/processing/resolved"
    }
    OP_LOG {
        int id PK
        int user_id FK
        string action
        string ip
    }
```

### 2.2 设计要点

1. 所有表含 `id`（自增主键）、`created_at`、`updated_at`；软删除统一 `is_deleted`，禁止物理 DELETE 业务数据；
2. 表间仅建**逻辑外键**（应用层保证一致性），不设物理外键约束，便于种子导入与软删除；
3. 时间字段一律 `DATETIME`（Asia/Shanghai 本地时间）；`traffic_flow.recorded_at` 建复合索引 `(road_section_id, recorded_at)` 支撑历史查询；
4. `signal_plan.phases` 使用 JSON 类型存相位数组 `[{name, green, yellow, allRed}]`。

---

## 3. 核心类图（后端服务层）

```mermaid
classDiagram
    class AlgorithmsDemo {
        <<module app/services/algorithms.py>>
    }
    class webster_calc {
        +phases: list[dict]$
        +dict 最优周期与绿灯
    }
    class congestion_level {
        +saturation: float$
        +int 0~3 四级
    }
    class calc_parking_fee {
        +minutes, free_minutes, first_hour_fee, hourly_fee, daily_cap$
        +float 费用
    }
    AlgorithmsDemo : 含 webster_calc / congestion_level / calc_parking_fee / speed_by_saturation / hour_flow_rate

    class AuthService {
        +get_user_role_code(db, user) str
        +get_user_menus(db, user) list
        +build_menu_tree(menus) list
    }
    class OpLogService {
        +log_op(db, request, user, action, detail)
    }
    class LprService {
        +preload_async() 后台预热
        +recognize_plate(bytes) dict
        -LPR_TIMEOUT_SECONDS = 10
    }

    class RouterAuth {
        +GET /auth/captcha
        +POST /auth/login
        +POST /auth/logout
    }
    class RouterSignal {
        +POST /signal-plans/calc
        +CRUD /signal-plans
    }
    class RouterParking {
        +POST /parking/enter
        +POST /parking/exit
    }
    class Deps {
        +get_db()
        +get_current_user()
        +require_roles(*roles)
    }

    webster_calc ..> AlgorithmsDemo
    congestion_level ..> AlgorithmsDemo
    calc_parking_fee ..> AlgorithmsDemo
    RouterAuth ..> AuthService : 调用
    RouterAuth ..> Deps : 依赖注入
    RouterSignal ..> webster_calc : 配时计算
    RouterParking ..> calc_parking_fee : 出场结算
    RouterAuth ..> OpLogService : 关键操作留痕
    RouterParking ..> LprService : 识别辅助
```

设计原则：**三个核心算法（配时/拥堵/计费）全部为纯函数**——不碰数据库、输入输出明确，前后端同源双实现（前端用于即时预览，后端为权威计算），两侧均有单元测试保证一致性。

---

## 4. 关键流程时序图

### 4.1 登录认证（含验证码、限流、JWT）

```mermaid
sequenceDiagram
    participant B as 浏览器(Login.vue)
    participant A as /api/v1/auth
    participant R as Redis
    participant D as MySQL

    B->>A: GET /auth/captcha
    A->>R: SETEX captcha:{uuid} 300s 码值
    A-->>B: {key, image(base64)}
    B->>A: POST /auth/login {username, password, captcha_key, captcha_code}
    A->>R: 校验锁定键 login:lock:{username/ip}
    alt 已锁定
        A-->>B: 429 {code:10005, 账号已锁定 10 分钟}
    end
    A->>R: GET+DEL captcha:{key}（一次性校验）
    alt 验证码不符
        A-->>B: 400 {code:10004}
    end
    A->>D: 查询 user（is_deleted=0）
    alt 密码错误(bcrypt verify 失败)
        A->>R: INCR login:fail:{name}（达 5 次→SETEX lock 600s）
        A-->>B: 401 {code:10002} / 429
    end
    A->>A: 签发 JWT（HS256，exp=2h，含 jti）
    A->>D: 写操作日志（登录）
    A-->>B: {token, role, user, menus 菜单树}
    B->>B: localStorage 持久化，跳转角色首页
```

登出流程：`POST /auth/logout` → 校验 token → `SETEX token:blacklist:{token} 剩余有效期` → 后续请求在 `get_current_user` 中被黑名单拦截（401）。

### 4.2 出入场计费

```mermaid
sequenceDiagram
    participant O as officer/admin
    participant P as /api/v1/parking
    participant D as MySQL
    participant F as uploads 目录

    O->>P: POST /parking/enter（multipart：lot_id, plate_no, file?）
    P->>P: 车牌正则校验（支持新能源 8 位）
    P->>D: 重复入场校验（status=inside 同牌同场）
    P->>D: 车位余量校验（used < total）
    P->>F: 保存入场拍照（≤5MB, jpg/png/webp）
    P->>D: 写 parking_record(inside) + used_slots+1 + 操作日志
    P-->>O: {record_id, plate_no, enter_time}

    O->>P: POST /parking/exit {lot_id, plate_no}
    P->>D: 查在场记录 + 关联 fee_rule
    P->>P: calc_parking_fee_dt(纯函数)<br/>免费时长→首小时→每小时(向上取整)→单日封顶(每24h一段)
    P->>D: 更新记录(exit_time, fee, finished) + used_slots-1
    P-->>O: {fee 结算金额}
```

### 4.3 Webster 配时计算

```mermaid
sequenceDiagram
    participant U as admin/officer
    participant FE as 前端 websterCalc(TS)
    participant A as POST /signal-plans/calc
    participant W as 后端 webster_calc(Python 纯函数)

    U->>FE: 输入各相位流量 q_i 与车道数 n_i
    FE->>FE: 本地预览计算（同源公式）
    FE->>A: {phases:[{name, flow, lanes}]}
    A->>W: 参数校验（Pydantic）
    W->>W: y_i = q_i/(s×n_i)，s=1800
    W->>W: Y=Σy_i；L=每相位6s
    alt Y ≥ 0.95（过饱和）
        W->>W: C0=180s，标记 oversaturated
    else
        W->>W: C0=(1.5L+5)/(1−Y)，钳制 [40,180]
    end
    W->>W: 绿灯=C0−L 按 y_i/Y 分配，最短 15s 约束迭代再分配
    W-->>FE: {cycle_seconds, phases:[{green, y}], oversaturated}
    FE-->>U: 展示结果，可保存为配时方案
```

### 4.4 大屏数据轮询（Redis 缓存）

```mermaid
sequenceDiagram
    participant BS as 大屏(10s 轮询)
    participant D as /dashboard/realtime
    participant R as Redis
    participant DB as MySQL

    BS->>D: GET /dashboard/realtime
    D->>R: GET dashboard:realtime
    alt 缓存命中（TTL 8s）
        D-->>BS: 缓存 JSON 直接返回
    else 未命中
        D->>DB: 聚合 6 组查询（24h 趋势/路口拥堵/信号分布/违章TOP5/停车占用/KPI）
        D->>R: SETEX dashboard:realtime 8s
        D-->>BS: 聚合结果
    end
    Note over BS: fetch 失败→全屏降级提示+重试按钮
```

---

## 5. 前端架构设计

### 5.1 目录与职责

```
frontend/src/
├── api/          # 按模块划分的接口封装（auth/traffic/vehicle/parking/service/dashboard）
├── stores/       # Pinia：auth（token/用户/菜单/角色）
├── router/       # 静态路由 + 登录守卫（meta.standalone 大屏独立全屏）
├── utils/        # algorithms.ts（Webster/计费 TS 同源实现）
├── views/        # login / layout / dashboard / system / traffic / vehicle / parking / tools / service / bigscreen
└── styles/       # 全局样式
```

### 5.2 权限与路由

- 登录后端返回该角色可见**菜单树**，侧边栏据此渲染；路由守卫仅校验登录态，页面级操作权限由后端 `require_roles` 兜底（前端隐藏入口 + 后端 403 双保险）；
- 大屏 `/big-screen` 为独立全屏路由（不套管理布局），按 1920×1080 设计稿 `scale = min(w/1920, h/1080)` 等比缩放（借鉴 go-view 方案）。

### 5.3 降级策略

Axios 响应拦截器统一处理：`code!==0` 弹错误提示；401 清理本地态跳登录页；网络异常提示「请检查后端服务是否启动」。大屏在轮询失败时展示全屏降级卡片并提供「重新连接」，恢复后 10s 定时器自动回填数据。

---

## 6. 后端架构设计

```
backend/app/
├── main.py          # 应用入口：CORS/统一异常/静态目录/路由注册/启动钩子
├── core/            # config(.env) / database / redis_client / security(JWT+bcrypt) / response(统一响应+错误码) / deps(认证与RBAC依赖)
├── models/          # 18 张表 SQLAlchemy 2.0 模型
├── schemas/         # Pydantic v2 请求模型
├── services/        # algorithms(纯函数) / auth_service / oplog / lpr_service
├── routers/         # auth / system / signal / traffic / violation / parking / lpr / service / dashboard / admin
├── tasks/           # APScheduler：每分钟流量模拟、每 10 秒信号灯相位推进（MOCK_DATA_ENABLED 开关）
└── utils/           # captcha 图形验证码 / validators 车牌校验与分页
```

关键机制：

1. **统一响应**：`BizError(code, message, http_status)` 由全局异常处理器转 `{code, message, data}`；RequestValidationError 转 422；
2. **认证依赖**：`get_current_user` 解析 Bearer token → Redis 黑名单校验 → 加载用户；`require_roles('admin')` 工厂做角色鉴权；
3. **模型预热**：启动钩子后台线程预载 HyperLPR3 模型（首次自动下载约 12MB），请求侧 10 秒超时保护，失败可重试、自动降级；
4. **静态回显**：`/static/uploads` 挂载 `backend/uploads/`，上传限 5MB、jpg/png/webp；
5. **报表导出**：`export` 路由 + `excel_service`（openpyxl）统一生成带标题/冻结表头/自动列宽的 xlsx，StreamingResponse 下载；
6. **数据治理**：`cleanup_flow_data` 每日 03:30 物理清理超过 `FLOW_RETENTION_DAYS`（默认 90 天）的流量时序数据（遥测数据生命周期管理，非业务单据，保留期可配置、0=禁用）。
